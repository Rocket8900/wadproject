import Connection from "./chatRoomModel.js"
import Message from "./chatRoomModel.js"

export default class ChatroomService {

    // details of connection and messages saved on mongodb (nosql)

    
    static getChatHistory = async (socketId) => {
        try { 

        } catch (error) {

        }
    }

    static saveMessageBasedOnChatroom = async (socketId) => {
        try {

        } catch (error) {
            
        }
    }


}